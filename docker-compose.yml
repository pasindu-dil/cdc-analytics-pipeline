
# ============================================================
#  Auto Sync: MySQL → Debezium → Kafka → ClickHouse
#  NO Consumer App needed!
#  ClickHouse directly reads from Kafka (built-in engine)
# ============================================================
 
networks:
  auto-net:
    driver: bridge
 
volumes:
  mysql-data:
  clickhouse-data:
  zookeeper-data:
  kafka-data:
  mongodb_data:
 
services:
 
  # ─────────────────────────────────────────
  # 1. ZOOKEEPER
  # ─────────────────────────────────────────
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zookeeper
    networks:
      - auto-net
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - zookeeper-data:/var/lib/zookeeper/data
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 5
 
  # ─────────────────────────────────────────
  # 2. KAFKA
  # ─────────────────────────────────────────
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka
    networks:
      - auto-net
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    volumes:
      - kafka-data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:29092"]
      interval: 15s
      timeout: 10s
      retries: 10
 
  # ─────────────────────────────────────────
  # 3. KAFKA UI
  # ─────────────────────────────────────────
  # kafka-ui:
  #   image: provectuslabs/kafka-ui:latest
  #   container_name: kafka-ui
  #   networks:
  #     - auto-net
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     KAFKA_CLUSTERS_0_NAME: local
  #     KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
 
  # ─────────────────────────────────────────
  # 4. MYSQL
  # ─────────────────────────────────────────
  # mysql:
  #   image: mysql:8.0
  #   container_name: mysql
  #   networks:
  #     - auto-net
  #   ports:
  #     - "3308:3306"
  #   environment:
  #     MYSQL_ROOT_PASSWORD: root
  #     MYSQL_DATABASE: bsmsc
  #     MYSQL_ROOT_USER: root
  #   command:
  #     - --server-id=1
  #     - --log-bin=mysql-bin
  #     - --binlog-format=ROW
  #     - --binlog-row-image=FULL
  #     - --expire-logs-days=10
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #     - ./mysql-init:/docker-entrypoint-initdb.d
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 10
 
  # ─────────────────────────────────────────
  # 5. DEBEZIUM KAFKA CONNECT
  # ─────────────────────────────────────────
  kafka-connect:
    image: debezium/connect:2.4
    container_name: kafka-connect
    networks:
      - auto-net
    depends_on:
      kafka:
        condition: service_healthy
      mysql:
        condition: service_healthy
    ports:
      - "8083:8083"
    environment:
      BOOTSTRAP_SERVERS: kafka:29092
      GROUP_ID: debezium-group
      CONFIG_STORAGE_TOPIC: debezium_configs
      OFFSET_STORAGE_TOPIC: debezium_offsets
      STATUS_STORAGE_TOPIC: debezium_statuses
      CONFIG_STORAGE_REPLICATION_FACTOR: 1
      OFFSET_STORAGE_REPLICATION_FACTOR: 1
      STATUS_STORAGE_REPLICATION_FACTOR: 1
      KEY_CONVERTER: org.apache.kafka.connect.json.JsonConverter
      VALUE_CONVERTER: org.apache.kafka.connect.json.JsonConverter
      KEY_CONVERTER_SCHEMAS_ENABLE: "false"
      VALUE_CONVERTER_SCHEMAS_ENABLE: "false"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/connectors"]
      interval: 15s
      timeout: 10s
      retries: 10
 
  # ─────────────────────────────────────────
  # 6. CLICKHOUSE (reads Kafka directly!)
  # ─────────────────────────────────────────
  clickhouse:
    image: clickhouse/clickhouse-server:24.1
    container_name: clickhouse
    networks:
      - auto-net
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      CLICKHOUSE_DB: bsmsc
      CLICKHOUSE_USER: root
      CLICKHOUSE_PASSWORD: root
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - ./clickhouse-init:/docker-entrypoint-initdb.d
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  
  # ─────────────────────────────────────────
  # 7. HyperDX (optional, for monitoring)
  # ─────────────────────────────────────────

  # hyperdx:
  #   image: docker.hyperdx.io/hyperdx/hyperdx:2
  #   container_name: hyperdx
  #   networks:
  #     - auto-net
  #   ports:
  #     - "8000:8000" #UI
  #     - "8001:8001" #API
  #   environment:
  #     FRONTEND_URL: http://localhost:8000
  #     HYPERDX_API_KEY: your_api_key_here
  #     HYPERDX_API_PORT: 8001
  #     HYPERDX_APP_PORT: 8000
  #     HYPERDX_APP_URL: http://localhost:8000
  #     HYPERDX_LOG_LEVEL: INFO
  #     MINER_API_URL: "http://miner:5123"
  #     MONGO_URI: "mongodb://mongodb:27017/hyperdx"
  #     SERVER_URL: http://127.0.0.1:8001
  #     OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4318"
  #     OTEL_SERVICE_NAME: "hdx-oss-app"
  #     USAGE_STATS_ENABLED: "true"
  #     DEFAULT_CONNECTIONS: '[{"name":"Local
  #       ClickHouse","host":"http://clickhouse:8123","username":"root","password":"root"}]'
  #     DEFAULT_SOURCES: >
  #       [
  #         {
  #           "from":{"databaseName":"default","tableName":"otel_logs"},
  #           "kind":"log",
  #           "timestampValueExpression":"TimestampTime",
  #           "name":"Logs",
  #           "displayedTimestampValueExpression":"Timestamp",
  #           "implicitColumnExpression":"Body",
  #           "serviceNameExpression":"ServiceName",
  #           "bodyExpression":"Body",
  #           "eventAttributesExpression":"LogAttributes",
  #           "resourceAttributesExpression":"ResourceAttributes",
  #           "defaultTableSelectExpression":"Timestamp,ServiceName,SeverityText,Body",
  #           "severityTextExpression":"SeverityText",
  #           "traceIdExpression":"TraceId",
  #           "spanIdExpression":"SpanId",
  #           "connection":"Local ClickHouse"
  #         },
  #         {
  #           "from":{"databaseName":"default","tableName":"otel_traces"},
  #           "kind":"trace",
  #           "timestampValueExpression":"Timestamp",
  #           "name":"Traces",
  #           "displayedTimestampValueExpression":"Timestamp",
  #           "implicitColumnExpression":"SpanName",
  #           "serviceNameExpression":"ServiceName",
  #           "eventAttributesExpression":"SpanAttributes",
  #           "resourceAttributesExpression":"ResourceAttributes",
  #           "defaultTableSelectExpression":"Timestamp,ServiceName,StatusCode,round(Duration/1e6),SpanName",
  #           "traceIdExpression":"TraceId",
  #           "spanIdExpression":"SpanId",
  #           "durationExpression":"Duration",
  #           "parentSpanIdExpression":"ParentSpanId",
  #           "spanNameExpression":"SpanName",
  #           "spanKindExpression":"SpanKind",
  #           "statusCodeExpression":"StatusCode",
  #           "connection":"Local ClickHouse"
  #         }
  #       ]
  #   volumes:
  #     - ./hyperdx-config:/app/config
  #   depends_on:
  #     - clickhouse
  #     - mysql
  #     - otel-collector
  #     - mongodb


  # otel-collector:
  #   image: otel/opentelemetry-collector-contrib:latest
  #   container_name: otel-collector
  #   command: ["--config=/etc/otel-collector-config.yml"]
  #   volumes:
  #     - ./hyperdx-config/otel-collector-config.yml:/etc/otel-collector-config.yml
  #   ports:
  #     - "4317:4317"
  #     - "4318:4318"
  #   networks:
  #     - auto-net
  #   depends_on:
  #     clickhouse:
  #       condition: service_healthy

  # mongodb:
  #   image: mongo:5.0.32-focal
  #   container_name: mongodb
  #   networks:
  #     - auto-net
  #   volumes:
  #     - mongodb_data:/data/db
  #   restart: unless-stopped
